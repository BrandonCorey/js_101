// Log all odd numbers from 1 to 99, inclusive, to the console, with each number on a separate line.
// Incluive will mean loop until < 100 or <= 100

for (let i = 0; i < 100; i++){
  if (i % 2 === 1) {
    console.log(i);
  }
}